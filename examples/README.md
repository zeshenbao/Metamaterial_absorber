# Examples

This folder contains small, runnable scripts that demonstrate the public API.

## basic_usage.py

Generates a triangle cross-section with a dot pattern and exports an STL.

Run:

```
python -m pip install -e ".[cadquery]"
python examples/basic_usage.py
```

Output: `triangle_dots.stl` in the project root.
